import 'dart:ui';

class Colours {
  const Colours._(); //make the constructor private so that it can't be instantiated
  static const primaryColor = Color(0xFF35693F);
  static const textColor = Color(0xFF1E1E1E);
  static const neutralTextColor = Color(0xFF414941);
  static const secondaryColor = Color(0xFFD4E8D1);
  static const noticeColor = Color(0xFFBA1A1A);
}
